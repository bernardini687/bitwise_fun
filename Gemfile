# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo| "https://github.com/#{repo}" }

# ruby '>= 2.2.2'

gem 'sinatra', '~> 2.0', '>= 2.0.7', require: 'sinatra/base'

group :test do
  gem 'rack-test', '~> 1.1'
  gem 'rspec',     '~> 3.8'
end
